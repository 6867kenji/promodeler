---
name: promodeler
description: Author or change realistic 3D assets with the promodeler Python API and headless Blender builds. Use when asked to model a prop, container, tool, material or scene piece in this repository.
---

Read `Skill/SKILL.md` at the repository root and follow it. It holds the authoring workflow (edit -> `recipe` -> `build` -> read `report.json` and the contact sheet -> edit), the API cheat sheet, the material and modifier rules that avoid wasted builds, and the verification and reporting contract.
